#include <stdio.h>
#include <ctype.h>
#include "main.h"
/**
 * mul - checks to see if c is is digit
 * @a: first num
 * @b: second num
 * Return: multiplication
 */
int mul(int a, int b)
{
int x = a * b;
return (x);
}

