#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

/**
 * _abs - Gets the positive of a number
 * @a: the int we will get its abs 
 * Return: the number in absolute form
 */
int _abs(int a)
{
return (abs(a));
}
