#include <stdio.h>
#include <string.h>
#include "main.h"
/**
 * _strlen - returns the value of the input string
 * @s: the string
 * Return: length
 */
int _strlen(char *s)
{
int x = strlen(s);
return (x);
}
